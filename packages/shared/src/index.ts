/**
 * @stackdock/shared
 * 
 * Shared TypeScript types and utilities for StackDock.
 * 
 * This package provides universal resource types that are provider-agnostic
 * and can be used across adapters, UI components, and Convex functions.
 */

// Re-export all types from schema
export * from "./schema.js"
